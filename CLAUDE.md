# ズレ計 — プロジェクト仕様書

政府・公的機関の「見通し」と「実績」を並べて記録し続ける静的サイト。
断罪も提言もしない。予測値・実績値・出典を毎年並べるだけの「計器」。

## 思想(変更しないこと)

- **思想は正直に、運用は無色に。** 動機はAboutページに全部書く。データページには形容詞を一切置かない。
- 政府批判サイトにしない。「楽観的すぎる」「ひどい」等の評価語はデータページ禁止。数字と出典リンクのみ。
- すべての数値に一次資料への出典リンクを付ける。出典のない数字は掲載しない。
- 目標は、公的な独立検証機関(日本版OBR/CBO)ができてこのサイトが不要になること。

## 情報公開の3段階

このプロジェクトの文書は、公開範囲に応じて3段階に分けて管理する。

1. **検討過程**: 設計や表現の迷い・比較検討そのもの。コミットしない(会話・作業メモのみ)。
2. **思想・動機**(多くの人に読んでほしい): なぜこのサイトを作るか。`about.md`に集約し、サイトのAboutページの原稿とする。
3. **実装・データの詳細**(監査・貢献者向け): フェーズ計画・データ設計・技術構成など、このCLAUDE.md本体や`data/README.md`のような形式仕様。一般読者向けの文章ではないが、検証可能性のために公開する。

## Aboutページ原稿

→ `about.md` を参照(確定済みドラフト)。

## フェーズ計画

### Phase 1: 政府経済見通し(実質・名目GDP成長率)

- 内閣府「経済見通しと経済財政運営の基本的態度」の当初見通し(毎年12月頃閣議了解/1月閣議決定)の成長率予測値を収集
- 対象期間: 可能な限り遡る(最低1980年度〜)
- 実績値: 内閣府「国民経済計算」の確報値
- 過去の見通しアーカイブ: https://www5.cao.go.jp/keizai1/mitoshi/mitoshikako.html
- 先行研究(検証の参考、数値の妥当性チェック用):
  - キヤノングローバル戦略研究所(2018): 名目成長率、1998-2017年度で的中5/20回
  - ニッセイ基礎研究所(2019): 実質成長率の予測誤差検証

### Phase 2: 税収見積もり

- 財務省の一般会計税収の当初予算見積もり vs 決算額
- 補正予算での修正履歴も記録できると望ましい

### Phase 3: 出生率・人口推計

- 社人研「日本の将来推計人口」の合計特殊出生率仮定(中位) vs 実績
- 同じ推計が置いている**出生数**(人数)の推計 vs 人口動態統計の実績(`/births`、2026-08-04公開)
- 歴代推計(1992, 1997, 2002, 2006, 2012, 2017, 2023...)を重ねた「扇形」グラフが目玉

## データ設計

- DBなし。リポジトリ内のCSVをソースオブトゥルースとする
- 形式例: `data/gdp_forecast.csv`
  - columns: fiscal_year, forecast_real, forecast_nominal, actual_real, actual_nominal, forecast_source_url, forecast_published_date, actual_source_url, notes, actual_basis, forecast_basis
  - actual_basis: 実績値がどの基準の系列か(`2015base-ref`=2015年基準の参考系列/`2020base-final`=2020年基準の確報/空=実績なし)。チャートで破線＋シーム線の描き分けに使う
  - forecast_basis: 見通しの概念基準(`gnp`=FY1993以前・国民総生産ベース/`gdp`=FY1994以降・国内総生産ベース)。チャートでGNPベース期を淡い背景帯で示すのに使う。gdp_forecast.csv以外の指標にはこの区別がないため空
- `data/gdp_vintages.csv`: 実質/名目GDPの基準別ヴィンテージ(平成2年〜2015年の6基準の後年推計成長率)。実質GDP/名目GDPページの背景「包絡帯」(各年の基準別最小〜最大)の元データ。実績値が基準改定でどれだけ動いたかを示す(Phase 2)。全系列GDPベース・出典はESRIの旧基準統計表。**1行=(年度 × 基準)の縦持ち**で、`basis` の語彙は `gdp_forecast.csv` の `actual_basis` と共通(呼称は `chart.js` の `T.basisLabels` が唯一の出所)。基準を足すのは行を足すことであって、列を足すことではない。詳細は `README.md`
- 実績値は改定されるため、取得日と確報/速報の別をnotesに記録
- 更新は年1回の手動運用を前提とする(自動化しない。壊れない仕組み優先)
- `csv.js`のパーサはクォート付きフィールド非対応の単純split(",")。自由記述欄(notes, reason等)に読点以外のカンマを含めない。カンマが必要な場面は「、」で代用する

## 技術構成

- 静的サイト。Cloudflare Pages(プロジェクト名 `zurekei-site`)
- ドメイン: zurekei.org をCloudflare Registrarで取得(取得〜DNS〜Pagesまで全部Cloudflareで完結)
- **デプロイはGitHub連携ではなくwranglerからの直接アップロード**。GitHubへのpushではデプロイされない。反映するには必ず下記を実行する
  ```
  npx wrangler pages deploy . --project-name=zurekei-site
  ```
  (Deployments画面にブランチ名とコミットメッセージが出るのはwranglerがgitメタデータを付けているだけで、Git連携ではない)
- アクセス解析: Cloudflare Web Analytics。Pagesプロジェクトの Metrics タブで有効化済みで、ビーコンは配信時に全HTMLへ自動挿入される。**有効化・設定変更は次回デプロイから反映される**ためデプロイを1回挟む
- フレームワーク: シンプル優先。Vite + React または素のHTML+JS。ビルドが複雑になるなら素のHTMLでよい
- グラフ: インタラクティブ必須(下記)

## デザイン方針

- コンセプトは「計器」。参照イメージは気象庁・日銀統計のような「そっけないのに信頼できる」佇まい。装飾やイラストではなく、数字の組版・余白・グリッドの精度で信頼を作る
- 数字が主役。等幅系で数字が美しいフォントを選ぶ(tabular numsを効かせる)
- **ウェブフォントは自前でホストする。第三者のCDNから読まない(2026-08-03)。** 以前は `style.css` の1行目で Google Fonts を `@import` していたが、それだと訪問者のブラウザが毎回 `fonts.googleapis.com` にリクエストを出し、IPアドレスが第三者に渡る。問い合わせメールの配信サービスを、本文に開封トラッキングのピクセルが入るという理由で乗り換えたことがある。それと同じ物差しで測ると噛み合わないので外した。いまは `assets/fonts/` の woff2 15本(IBM Plex Mono 3ウェイト×5サブセット、計98,580バイト=約96KB)を `@font-face` で読む。`unicode-range` は Google Fonts が返す CSS から写したまま保つ(必要なサブセットだけが取得される)。**日本語のウェブフォント(IBM Plex Sans JP)は載せない**: 全ウェイトで6.5MBあり、`node_modules` を履歴から消して 1.1MB まで絞ったこのリポジトリに入れる釣り合いが取れない。加えて IBM Plex Mono に日本語グリフは無く、`.mono` を当てている箇所(カード名・表・ラベル等、サイトの日本語の大半)はもともとシステムフォントで描かれていたので、本文もシステムフォントに寄せたことでむしろ不揃いが消えている。**フォントを足すときも同じ規則**: CDNの `@import`/`<link>` は使わず、ファイルをリポジトリに置いて `@font-face` で読む(`preconnect` も同時に消すこと。2026-08-03に `@import` だけ消して `preconnect` を全ページに残し、目的を達しないまま完了しかけた)
- **開幕10秒の演出(サイトの目玉)**: トップページを開くと、実績の線がすっと引かれ、歴代の予測線が1本ずつ重なり、ズレが扇形に開いていく。文章ゼロで主張が伝わるアニメーション。OGP画像はこの完成形のグラフ(SNSで流れてきた人に絵で刺さる)
- prefers-reduced-motion では静的表示にフォールバック
- 内容が地味なぶん、見せ方は動的に:
  - 年を選ぶスライダー/セレクタで「その年に政府が見ていた未来」の予測線がアニメーションで描かれ、実績線と重なる
  - 予測vs実績の乖離(誤差)が選択に応じて動的に表示される
  - Phase 3の出生率は歴代推計を1本ずつ重ねていく「扇が開く」演出が目玉
- 評価語なし。色で善悪を示唆しない(赤=悪、のような色使いは避け、予測/実績の区別のみに色を使う)
- モバイルファースト(SNSシェアからの流入想定)
- OGP画像はグラフそのもの(シェアされたときに絵で伝わる)。**2026-07-30から指標ごとに専用のカードを持つ**: 汎用2枚(`assets/og.png` / `assets/og-en.png`、トップ・`/chart`一覧・about/corrections/contact等が使う)+指標8種×2言語=`assets/og-<key>.png` / `assets/og-<key>-en.png`の16枚+扇形ページ2種×2言語(`/fertility`の`assets/og-fertility[-en].png`と、2026-08-04に足した`/births`の`assets/og-births[-en].png`)の4枚=計22枚。生成元はどれも`og.html`(サイト本体からはリンクしない道具ページ)で、`?lang=en`で英語カード、`?m=<chart.jsのMETRICSキー>`でその指標専用のカード(省略時は従来どおり実質GDPの汎用カード)を描く。指標定義(タイトル・CSV・列名・単位)はog.html側に複製を持たず`chart.js`の`METRICS`を唯一の出所として読む。`/fertility`と`/births`は`?m=fertility` / `?m=births`という予約値で、`chart.js`の`METRICS`ではなく歴代推計(扇)+実績の専用描画経路(`renderFan()`)を通る(トップの開幕演出と同じ「歴代推計を重ねた扇」を静止画にしたもの)。**予約値の一覧と、2種の違い(CSV・値の列名・y軸の規則)は`og.html`の`FAN_SPECS`が唯一の出所**で、`main()`の分岐も`FAN_SPECS[m]`を引くだけ(予約値を別途列挙しないので「キーはあるのに描けない」状態を作れない)。フッタの年表記は他の指標が「年度」なのに対しこの2種だけ「年」(どちらのCSVも`fiscal_year`ではなく`year`/`target_year`で管理しているため)。y軸だけはページごとに規則が本当に違うので`FAN_SPECS`の`yDomain`が持つ(出生率は`fertility.js`と同じ「最小値に1を含めて上下8%パディング」、出生数は`births.js`と同じ「200000の倍数に丸め、パディングなし」)。**出生数カードは`＊`(総人口ベースの版に付く印)を描かない**——カードは版ラベルを一切描かないので印を付ける相手が居ないうえ、基準の差(2021年で約1.8万人)はこのy軸では1px相当で線としては見えず、数値も一切書かないため、カードだけを見て基準を取り違える読み方が生じない(基準の話はページ本文の`scopeNote`が唯一の出所であり続ける)。**`/hoan`は依然として描画が別系統のため対象外**で、汎用カードのまま(表であってグラフではないため、この仕組みでカード化する対象にそもそもならない)。データや`chart.js`・扇形カードのCSV4本(`fertility_*.csv` / `births_*.csv`)・`og.html`を直したら`bin/og.sh`で焼き直す(1コマンドで22枚とも書き出し、1枚でも2400x1260でなければ非0終了する)。**レンダリングは決定的なので、関係ないカードは焼き直してもバイト同一になる**——`/births`のカードを足したとき、既存20枚が`git diff`で1バイトも動かないことをもって「他のカードに影響していない」を確認した(22枚を目で見比べるより強い確認になる。目視が要るのは新しく増えた枚数だけ)。焼き直し忘れの検出は「運用ルール」を参照

## 運用ルール

- 年次更新: 政府経済見通し(12-1月)、決算・確報(7-12月)、人口推計(不定期)
- 訂正ポリシー: 誤りは履歴を残して訂正(黙って直さない)。訂正履歴ページを設ける
- 問い合わせ窓口: `/contact` のフォーム。全ページのフッターとAboutページから導線を張っている。運営は一個人であることをAboutに明記する(それ以上の属性は書かない)
- ライセンス(2026-07-29決定): データ・本文はCC BY 4.0(`LICENSE-DATA`)、コードはMIT(`LICENSE`)。著作権者表記は「ズレ計 (zurekei.org)」で個人名・メールは書かない。適用範囲は「サイトが作った部分」に限り、政府公表の数値そのものと`data/hoan_clauses/*.txt`の条文原文(著作権法13条によりそもそも著作権の対象外)には及ばない。分け方の理由は`NOTICE`、詳細は`data/README.md`の「7. ライセンス」、サイト上の表記は`about.js`の`licenseBody`。**`LICENSE`はMITの原文だけにしておくこと**(説明を足すとGitHubのライセンス自動判定が外れ、リポジトリにライセンス表示が出なくなる。だから説明は`NOTICE`側に置いている)。各所の対象リストは「網羅一覧」ではなく例示として書く(限定列挙に読めると、挙げ忘れたものを自ら権利放棄したように見える)
- **`chart/*.html` は生成物。直接編集しない。** `bin/build.mjs` が `chart.js` の `METRICS` と `data/*.csv` から書き出している。直すのはそのどちらか
- **`fertility.html` / `births.html` / `hoan.html` / `corrections.html` の `<!-- BUILD:名前 -->` … `<!-- /BUILD:名前 -->` の内側も生成物。直接編集しない。** これらは丸ごと生成ではなく、数値やJSON-LDの入る領域だけを機械の持ち物にしている(URLも版面も元からあるため)。マーカーの外は手で触ってよい。`corrections.html` は元は `about.html` / `contact.html` と同じ純粋な静的ファイルだったが、`#corrections-list` だけは(訂正が1件でも記録された瞬間、JSを実行しないクローラにはJA版の訂正履歴だけが空に見えてしまうのを防ぐため)2026-07-30にこの方式に切り替えた
- **`index.html` は「マーカーの外は手で触ってよい」ページではない。** JSON-LDの領域は他ページと同じ `<!-- BUILD:jsonld -->` マーカーだが、それとは別に24個の翻訳対象要素(`id="t-tag"` 等)がマーカー無しでid単位まるごと機械の持ち物になっている(2026-07-30)。文言の原本は `home.js` の `T` 辞書で、`index.html` 側のテキストを直接書き換えても、そのidが機械の持ち物である限り次の `node bin/build.mjs` で黙って元に戻る。**文言を変えるときは `home.js` の `T` を直してから `node bin/build.mjs` を実行すること**
- **JSON-LDの`Dataset`は、1ページだけを見て完結する形で書く。`description`は50字以上にする(2026-08-01)。** どちらもSearch Consoleで実際に無効判定を食らって分かったこと。(1) トップと`/en/`の`DataCatalog`は10指標を`dataset`配列に並べているが、当初は`@id`/`name`/`url`/`license`だけの省略形にして`description`と`creator`は各指標ページ側のフル定義(同じ`@id`)に持たせていた。`@id`が同じなら同一ノード、というのはJSON-LDの仕様としては正しいが、**Googleは1ページ単位で検証していて文書をまたいだ統合をしない**ため、省略形が「必須項目の欠けたDataset」として10件×2言語=20件まるごと弾かれていた(2026-07-31検出)。省略形にしないこと。(2) それを直した翌日の再クロールでも6件が無効のまま残り、合否は`description`の文字数で完全に分かれていた(有効: 75/77/77/118字、無効: 23/23/27/27/34/34字)。**Googleの`Dataset`は`description`に50字以上を求める。** `desc`は`chart.js`の`METRICS`から`meta description`・`og:description`・`/chart/`一覧・ページ副題・JSON-LDのすべてに伝わるので、**指標を足すときは`desc`を50字以上で書く**こと。無効側6件はいずれも「政府の当初見通しと、確定した実績を並べたもの。」のような汎用文で、どの機関のどの資料かが書かれていなかった(`完全失業率`と`経常収支`にいたっては1文字も違わず、検索結果に同じ文が並ぶ状態だった)。**見通しの出どころと実績の出どころを書けば自然に50字を超える** — 有効判定だった4件はもともとそう書かれていた。字数を稼ぐために引き伸ばすのではなく、足りない情報を足すのが正しい直し方
- **`sitemap.xml` も2026-07-29から生成物。直接編集しない。** `bin/build.mjs` が `METRICS` からURLを、各ページの元ファイルの git コミット日から `lastmod` を導く。直すのは `chart.js` の `METRICS` か、依存ファイルの対応表(`bin/build.mjs` の `sitemapEntries()`)
- **JSから `data/` を取るパスは必ずルート絶対(`/data/...`)で書く。相対で書くと `/en/` 配下で静かに壊れる(2026-08-01)。** `/en/hoan` から `data/hoan_review.csv` を相対で取ると `/en/data/hoan_review.csv` を叩くことになるが、**Cloudflare Pages は存在しないパスにも 200 でトップページのHTMLを返す**ため、`res.ok` は真になり、HTMLをCSVとしてパースした結果がそのまま描画される。2026-08-01まで `hero.js` / `home.js` / `fertility.js` / `hoan.js` / `corrections.js` が相対のままで、`/en/`・`/en/fertility`・`/en/hoan`・`/en/corrections` の4ページが実際にこれを踏んでいた(`/en/hoan` は「273 laws」全行 `undefined` の表、`/en/` のキャプションは `FYNaN–NaN`)。JAでは同じ相対パスがたまたま正しく解決するので**JA側を見ている限り永久に気づかない**のが質の悪いところ。`csv.js` の `loadCSV()` は本文が `<` で始まったら throw する(`res.ok` だけでは検出できないため)。`fetch` を直に書く箇所(`hoan.js` の条文txt)にも同じ判定を置くこと
- **`about.html` / `corrections.html` / `contact.html` の手書き部分(`index.html` と違いマーカーもid単位の機械管理も無い純粋な地の文)は、対応する `.js` の `T.ja` と文言が一致していなければならない。** `--check` は各 `.js` の `document.getElementById(id).textContent/innerHTML = t.key`(または `set(id, t.key)` ヘルパー経由)の形の代入を静的に読み取り、HTML側の同じidの中身と突き合わせて食い違いを検出する。T.ja側だけ直してHTMLを直し忘れても、ブラウザ(JSが動く側)では正しい文言に上書き表示されるため、この検査が無いと誰にも気づかれない
- **`index.html` の24個のid別代入(`home.js` の `applyI18n()`)は `bin/build.mjs` の `HOME_FILLS` に手書きで写してある。** `home.js` に代入を1行足して `HOME_FILLS` に足し忘れると、id存在チェック・生成物の陳腐化チェック・上記の手書きJAページの文言チェックのどれにも掛からず、JAトップに空要素が再発する。`--check` は `home.js` から代入パターンを直接抽出し、`HOME_FILLS` の (id, key) の組と双方向で突き合わせる
- **文言・定義は各ページの `.js` にしか書かない。** ビルド側は `chart.js` / `fertility.js` / `births.js` / `hoan.js` / `csv.js` をそのまま評価して本物の定義・整形関数・`T`(辞書)を取り込むので、写しは存在しない。写しを作ると、いつか必ず「表とグラフが同じ年に違う数字を出す」状態になる
- **CSVを直したら `node bin/build.mjs` を回す。** 忘れた場合は `bin/deploy.sh` が `--check` で止める(生成物の陳腐化・sitemapとトップのリンクの取りこぼしを同時に見る)。手順の記憶に頼らない
- **OGP画像(`assets/og*.png`、汎用2枚+指標8種×2言語の16枚+扇形2種×2言語の4枚=計22枚)は `og.html` を焼いた画像で、`bin/build.mjs` の生成物ではない(人間が `bin/og.sh` で焼き直す)。** 焼き直し忘れで実際に事故が起きたことがある: FY1980〜1997の見通しを収集して `data/gdp_forecast.csv` を伸ばした後もこれを忘れ、破線が1998年からしか無い古い画像が公開され続けていた(2026-07-30発覚)。`--check` はこれを検出するため、`bin/og.sh` が焼くたびに書く**入力ハッシュのスタンプファイル**(`assets/og.inputs.json`。path→sha256の対応表で人間可読、site配下にあるため配信されるが秘密は含まない)を、いまの入力ファイルの実ハッシュと突き合わせる。対象は `chart.js`(2026-07-30、指標ごとのOGカード対応でog.htmlがMETRICSをfetchして読むようになったため追加)・`METRICS`の全指標が使うCSV全部(重複除去。手書きのリストではなく`bin/build.mjs`が`METRICS`から機械的に導くので、指標を足せば自動で対象に入る)・扇形カードのCSV4本(`data/fertility_*.csv`は2026-07-30、`data/births_*.csv`は2026-08-04。`bin/build.mjs`の`FAN_CARDS`から機械的に導くので、扇形ページを足せば自動で対象に入る。`fertility.js` / `births.js` 自体は入れていない。og.htmlの`?m=fertility` / `?m=births`経路はCSVを直接fetchして自前で扇+実績を描くだけでページ側のコードは一切読み込まないため、対象にすると実際には効かない入力を監視することになり無意味)・`og.html` / `csv.js`(og.htmlが`loadCSV`/`toNum`をここから読み込んでいる) / `style.css`。この入力集合(`bin/build.mjs`の`OG_INPUT_RELS`)は`node bin/build.mjs --og-list`が唯一の出所で、`bin/og.sh`は自分で同じリストを手書きしない(以前は`bin/og.sh`側にも`INPUT_RELS`という手書きの配列があり「片方だけ増減すると`--check`が気づく」という二重管理だったが、2026-07-30にそもそも1箇所にする設計へ変えた)。**git のコミット日時もファイルのmtimeも一切使わない。** 以前は「実効的な最終更新時刻」(コミット済みでクリーンなパスはgitのコミット日時、未コミットのパスはmtime)を比較する設計だったが、再現可能なデッドロックが見つかり2026-07-30に撤回した: 描画に効かない変更(例: CSVの`notes`列を1文字変える)をしても、`bin/og.sh`で焼き直した画像は決定的レンダリングによりバイト同一になるため「dirty」にならず、mtimeも自然には更新されない。一度この状態に入ると二度と `--check` を通せなくなる(`git commit --amend`・rebase・squashや `cp -p`/`rsync -a` のようなmtime温存コピーでも同種の誤判定が起きる)。ハッシュスタンプはPNGのバイト列ではなくスタンプファイル自体の中身が焼き直しのたびに必ず変わるため、この種のデッドロックが構造的に起きない(2026-07-30に実際に「CSVのnotesを1文字変える→`--check`が発火→`bin/og.sh`で焼き直す→PNGはバイト同一だがスタンプは変わる→`--check`が通る」という手順で確認済み)。**既知の穴だった`style.css`除外もこのとき解消した**: 以前は「比較対象に含めるとCSSを触るたびに無関係に発火して形骸化する」という理由で外していたが、ハッシュ方式では発火は「一度`bin/og.sh`で焼き直せば必ず解消する」だけなので、その除外理由は成立しなくなった。fail-closed: スタンプが無い・壊れている、またはスタンプが記録している入力パス集合といまの `OG_INPUT_RELS` が食い違う(入力を増減したのに焼き直していない)場合はいずれもエラーで止まる。**`assets/`の余分なファイル・不足ファイルの検査も別枠である。** 「焼くべき22枚」の集合(`bin/build.mjs`の`OG_CARDS`、指標分は`METRICS`から、扇形分は`FAN_CARDS`から機械的に導く。もし将来`METRICS`に`fertility`や`births`というキーを足してしまっても、`og-fertility.png` / `og-births.png`という出力先パスが指標分と衝突するため既存の一意性検査がエラーで捕まえる。これは2026-07-30に実際に一時的に`METRICS.fertility`を足して確認済み)と実際に`assets/`にある`og*.png`が一致しない場合はエラーになる(指標を消したときに古いカードが配信され続ける事故や、`bin/og.sh`を経ずに置かれた置き土産を検出する)。`--og-list`のCARD行には2026-07-30から種別列(`metric`/`fan`)が入っている(2026-08-04に`fertility`→`fan`へ改名した。種別が表しているのは「どのページか」ではなく「扇形の描画か」なので、ページ名で名乗ると扇形が2枚目になった時点で内容と合わなくなる)。`bin/og.sh`はこれで検証方法を分岐する(下記)。**この検査でも機械的に捕まえられない穴は残っている**: (1) ~~フォールバックフォント~~ → **2026-08-01に塞いだ。** `IBM Plex Mono` は**ウェブフォント**でシステムには入っていないため、当たらなければフォールバックの素のサンセリフで焼ける。当時は `style.css` の1行目が Google Fonts を `@import` しており、取得が `--virtual-time-budget` の内に間に合わないのが壊れ方だった(2026-08-03に自前ホストへ移してこの経路自体は消えたが、`src` のパス違い・ファイルの取り違え・`unicode-range` の書き損じで同じ結果になるため検査は残してある)。実際に `assets/og-fertility-en.png` がそうなり、データと線は正しいのにワードマークだけがサンセリフになったまま公開直前まで気づかなかった(2枚を目視比較して初めて見つかった)。いまは `og.html` の末尾が `document.fonts.ready` を待ってから**同じ文字列を `"IBM Plex Mono", monospace` と `monospace` だけで描いて幅を比べ**、1つでも変わらなければ `data-font-ok="0"` を立て、`bin/og.sh` の `verify_dom` がそれを見て止める(fail-closed)。**`document.fonts.check()` は使ってはいけない**: 宣言の無いフォント名を渡すとフォールバックで描けるので `true` を返す仕様で、`false` になるのは「宣言済みのウェブフォントがまだ読み込み中」のときだけ。つまり `@import` 自体が失敗して `FontFace` が1つも作られなかった場合(=当時実際に起きた壊れ方)には `true` を返し、何も検出しない。一度この実装で書き、存在しないフォント名に差し替える負のテストで20枚とも素通りすることを確認して捨てた。**測定は宣言しているウェイト(400/500/600)ごとに行う。** 2026-08-03まで400の1回しか測っておらず、自前ホスト化で「600の3本だけ`src`を打ち間違える」という部分故障が起こり得るようになったのに、ワードマーク(`.og-brand-name`)は600なので**2026-08-01とまったく同じ絵が素通りする**状態だった(同日のレビューで指摘され修正)。`@import`はall-or-nothingで落ちるため400だけで足りていた、という前提が自前ホストで崩れた例。幅を測る方式は正負両方向で実測してある(正: 20枚完走。負: 測定用の1行を存在しないフォント名に差し替える/600のwoff2だけ`src`を壊す、のどちらでも1枚目で停止)。(2) ヘッドレスChromeのバージョン差によるレンダリング差と、**焼く機械のシステムフォント差**(2026-08-03に増えた穴。それ以前はカード上の日本語も `IBM Plex Sans JP` のウェブフォントで焼かれOSに依存しなかったが、日本語ウェブフォントをやめたので日本語部分はいま焼く機械のシステムフォント次第になる。同じMacで焼き続けている限り実害は無いが、別マシンで焼くと絵が変わる)。また **`IBM Plex Mono` を焼く機械にローカルインストールしてはいけない**: 下記の幅測定はローカルの同名フォントで描けてしまうと通るので、webfontが壊れていても素通りする。(3) y軸ラベルのはみ出し — `og.html`はラベル幅を実測ではなく文字数から見積もって`PAD.left`(左余白)を広げているため、見積もりが外れて枠外にはみ出す可能性がある。DOM自体のテキストは正しいままなので機械検査では検出できず、焼いた画像を目で見て確認するしかない。(4) `assets/og*.png` を `bin/og.sh` を通さず直接上書きした場合の「中身」の正しさ。このスタンプは入力ファイルのハッシュしか記録せず出力画像のハッシュは記録していないため、`--check` は気づかない(ファイルの過不足は上記の別枠の検査で見るが、中身の正しさまでは見ない)。`bin/og.sh` は焼くたびに `--dump-dom` でフッタの年度ラベル(カードごとの期待最小年度は`--og-list`から受け取る)と実績線(`line-actual`)のSVGパスにデータが入っていることも見るが、これも画像自体の正しさを保証するものではない(2400×1260という寸法は確認するが、フォント代替やラベルのはみ出しのような中身の描画ズレは対象外)。**扇形の4枚は種別が`metric`と違う(`fan`)ため、この`--dump-dom`検証も同じにはできない。** 予測線を表す`line-forecast`が扇形のカードにはそもそも存在しない(歴代推計の扇は`line-forecast-vintage`が複数本というまったく別の構造)ので、種別`fan`のカードでは`line-forecast`の有無ではなく実績線(`line-actual`)にデータがあることに加えて、**扇の本数が`--og-list`の`vintageCount`(そのカードのCSVから導く歴代推計の版数)と一致すること**を見る(1本でも描き漏れれば検出できる、この機能追加でいちばん重要な検査)。この検査は出生率カードと出生数カードで完全に同一(扇の本数を数えるだけで、どちらのCSVかには依存しない)。種別が`metric`でも`fan`でもない値が来た場合は`bin/og.sh`はエラーで止まる(fail-closed、未知の種別を黙って通さない)。**値の列名は`og.html`の`FAN_SPECS`と`bin/build.mjs`の`FAN_CARDS`の両方に書いてある**(og.htmlはHTMLなので`loadModule()`で読めず、辞書と違って共有経路が作れない)。ずれれば`vintageCount`の期待値が実際の描画と食い違って必ず赤くなるが、**列名の綴り違いだけは両側が揃って0本と数えて`0==0`で通る**ため、両側とも「その列がCSVのヘッダに実在するか」を先に見て投げる(2026-08-04追加。`toNum()`が存在しない列を`null`にすることに由来する穴)
- **CSVの「空でない非数値セル」は必ずエラーにする(2026-07-30)。** `csv.js`の`toNum()`は空文字だけを`null`にし、転記ミス(`1.49x934`のような打ち間違い・全角数字・単位の混入)には`NaN`を返す。呼び出し側のフィルタは`!== null`なので`NaN`は素通りし、**1セル汚れるだけで`Math.min`/`Math.max`経由でy軸の定義域が非数になり、扇7本と実績線の全pathが座標に`NaN`を持つ形になって1本も描かれない**。にもかかわらず、フッタの年ラベル(x軸の定義域は無傷)も、線の本数の検査(`d="M <数字>`というパターンは**最初のx座標しか見ない**)も、`bin/build.mjs`側の`vintageCount`(同じ`!== null`で数える)も、すべて自己整合的に「正常」と答えるため、**真っ白なカードが全検査を通過して配信されうる**状態だった(2026-07-30に実際に汚して再現を確認)。データの転記ミスはこのサイトで最もありそうな壊れ方なので、二重に塞いである: (1) `og.html`の`renderFan()`と`bin/build.mjs`の`fanCardInfo()`が同じ規則で`Number.isFinite`を検査し、空でないのに数値として読めないセルは**黙って捨てずに列名つきでthrowする**(黙って捨てる実装にすると、今度は両側で本数が揃ってしまい「1本欠けた扇」が静かに通る)。(2) `bin/og.sh`の`verify_dom`が`--dump-dom`のDOM側で**SVG要素の属性値に`NaN`が混ざっていないか**を種別によらず全カードで見る(指標カードの途中混入も同時に塞げる)。なお`--dump-dom`は`<script>`の中身もDOMとして吐くため、**`og.html`のコメントに壊れた`d`属性の実例を文字列として書くとこの検査が自分のコメントにマッチして全カードで誤爆する**(2026-07-30に実際に踏んだ。og.html側にも注記を残した)。(当初この網はOGカードの経路にしか無く、`/chart`系や`/fertility`のページ自体は転記ミス1文字で線が消えても`--check`が緑のままだった。同日中に下の`csvSchemaErrors()`で全CSVに広げた)
- **`data/*.csv`のスキーマ検査(`bin/build.mjs`の`csvSchemaErrors()`、2026-07-30)。** 上のNaN対策をOGカードの経路だけでなく**全CSVに広げたもの**で、`--check`の一部として4つを見る。(1) **列の型宣言**: `CSV_COLUMNS`が全12ファイルの全列を`"number"`(空でないセルは10進表記でなければならない)か`"text"`(検査しない)に分類する。(2) **網羅性(fail-closed)**: `data/`配下にあるCSVが宣言に無い・宣言があるのに実ファイルが無い・ヘッダの列が分類されていない・宣言にある列がヘッダに無い・**同じ列名がヘッダに複数回ある**、のいずれもエラー。**新しいCSVや列を足して分類し忘れる穴が構造的に残らない。** 探索は`data/`を**再帰的に**、拡張子は**大文字小文字を区別せず**行う(当初は直下のみ・`endsWith(".csv")`だったため、`data/EXTRA.CSV`のような大文字拡張子とサブフォルダに置いたCSVが検査対象にすらならず、非数値セルを含んだまま`--check`が完全に緑になっていた。`wrangler pages deploy .`はディレクトリを丸ごと上げるので、検査されないCSVがそのまま公開される)。列名の重複を見るのは、`parseCSV`が`row[h] = cells[i]`で**後の出現が勝つ**ため、重複した列の値が本来の列を丸ごと上書きするのに、集合で見る他の検査は重複が潰れて素通りするから(実測では、ヘッダに既存の`forecast_tn`をもう1つ足して全行に`999.9`を付けると、スキーマ検査の指摘は0件のまま`chart/tax-revenue.html`に`999.9`が48箇所焼き込まれた。**汚染データが正規の手順で緑のまま配信される唯一の実証済み経路**だった)。宣言の値が`"number"`/`"text"`以外(タイポ)の場合もエラーにする — `!== "number"`で分岐しているだけだと、`"numer"`と書いた列が黙って検査対象から外れる。 (3) **行の列数**: `csv.js`の`parseCSV`は素の`line.split(",")`で引用符もエスケープも扱わないため、`notes`に読点代わりのカンマを1個書くだけで以降の列が全部ズレて静かに壊れる。ヘッダの列数と一致しない行をエラーにする。(4) **数値セル**: ファイル名・行番号・列名・実際の値を出して止める。ただし`data/fertility_forecast.csv`と`data/fertility_actual.csv`の2本だけは、この検査に到達する前にモジュール評価中の`fanCardInfo()`(OGカードの扇の本数を数えるために同じCSVを読む)がthrowするため、**止まりはするが出るのは行番号なしのスタックトレース**になる(ファイル名と列名は出る)。ゲートが閉じることは変わらないので診断の見やすさの問題として許容している。**判定は`Number.isFinite(Number(cell))`ではなく正規表現`/^-?(0|[1-9][0-9]*)(\.[0-9]+)?$/`で行う** — `Number()`は`0x10`を16、`1e5`を100000、`+1.5`や前後に空白のある`" 1.5"`もそのまま受け取るため、**転記ミスが「別の値」として黙って集計に入る**のがいちばん危ない(現行の数値セル3153件は全件がこの厳しい形に当てはまることを実測してから締めたので、誤爆はしない)。**自動判別(「その列に数値セルが1つでもあれば数値列とみなす」等)にはしていない**: `gdp_forecast.csv`の`actual_basis`(`2015base-ref`等)と`forecast_basis`(`gdp`/`gnp`)は`forecast_`/`actual_`という接頭辞を持つのに非数値で、接頭辞で機械的に分類すると**この2列だけで91件の誤検出が出て`--check`が恒久的に赤になる**(実測)。正当なデータで赤が固定する検査はこのリポジトリでは安全性の欠陥として扱う(デプロイゲートが閉じ、人が素の`npx wrangler pages deploy`に逃げて機密ファイル混入スキャンを迂回する)。**`csv.js`の`toNum()`自体は変えていない**: ブラウザ側でthrowさせると1セルの転記ミスが訪問者に真っ白なページを出すことになるので、デプロイゲートで止めるほうが訪問者に影響を出さずに防げる。**この検査で捕まえられないもの**: 数値として妥当だが値が間違っている転記ミス(`1.53`を`1.35`と書き間違えた場合)は検出できない。`"text"`列の中身(URLの形・日付の妥当性)も見ていない。ここは出典との人間のダブルチェックが唯一の防波堤であることに変わりはない
- **JSでしか描かないページを新しく作らない。** ブラウザでは正しく見えるので気づけないが、JSを実行しないクローラにとっては読む中身が無い。出典つきで確かめられることを主旨に置いている以上、確かめる相手がHTMLしか読まないなら、HTMLに数値が入っていなければ意味がない。新しいページを足すときは、同じように数値をHTMLに書き出す経路を `bin/build.mjs` に足す
- **JSが後から書き換える領域に静的な中身を置くときは、JS無しでも操作できる形にする。** `hoan.html` の条文原文は、JS版が行クリックで開く仕組みなのに対し、静的版は `<details>` を使っている。JS版の `hidden` をそのまま静的側に持ち込むと「HTMLには入っているが決して開けない」状態になる
- **出典も静的HTMLに入れる。上の「数値を入れる」と同格の要求で、数値だけ入れても足りない(2026-08-04)。** 「出典つきで確かめられる」の**出典つき**の側で、確かめる相手がHTMLしか読まないなら、出典がJSでしか組み立てられていないのは数値が無いのと同じこと。`/fertility` が実際にそうなっていて、実績値の出典3件とスキャンPDFの但し書きがJSを実行しない相手に届いていなかった。`--check` の `sourceUrlReachErrors()` が `data/*.csv` のURL列を全部拾い、サイトのどれかの静的HTMLに現れることを要求する(fail-closed。0件もエラー)。**表の形が「行=年」でないページを足すときは要注意** — `/chart/*` は行ごとに出典欄を持てるので自動的に満たされるが、`/fertility` のような「年 × 版」の行列では持てず、出典の載せ場所を別に決める必要がある。**この検査は「CSVのURL列に載っているURL」しか見ないので、URL列を持たないCSVに対しては何も要求しない**(検査対象が0件なので緑のまま通る。全体で0件のときだけエラーにする作りで、ファイル単位の0件は素通りする)。2026-08-04に `gdp_vintages.csv` にURL列を足したところ、12本中10本が「静的HTMLに無い」状態だった — 背景の包絡帯の出典は、それまでどのページにも1本も出ていなかった。**出典列の欠落は、出典の欠落を検査から隠す**。数値を載せるCSVに出典列が無いと気づいたら、それは1つではなく2つの穴だと考えること
- **`/chart/*` を通らないページ(`/fertility`・`/births`)を足すときは、`chart.js` の `METRICS` ではなく `bin/build.mjs` の `REL` にURLを持たせる。** `chartRel()` は `/chart/<key>` しか作れないので、トップの `card-fallback` を組み立てる `buildHomeEn()` の `NON_CHART_REL` と、`cite.js` の `PAGE_PATH` にも同じ組を足すことになる。JA側の `index.html` の `card-fallback` は手書きなので、`home.js` の `INDICATOR_META` と並び順込みで揃える(`cardFallbackErrors()` が見る)
- **指標を増やしたら `chart.js` の `METRICS` と `index.html` の素のリンクの2箇所。** `sitemap.xml` は2026-07-29から `bin/build.mjs` の生成物になり `METRICS` からURLを自動で導くようになったため、手で直す対象から外れた。`index.html` は今も手書きなので、足し忘れても見た目には何も起きず静かに索引から漏れる。`--check` がこの取りこぼしを検出する
- **EN側で、一次資料からの転記を訳さない。訳せるのは「換算で決まるもの」だけ(2026-08-03)。** `/hoan` の法令名・法令番号・条文原文はいずれも一次資料そのままなので、EN でも日本語で出し `lang="ja"` を付ける(`bin/build.mjs` の静的版と `hoan.js` のJS版の**両方**に付けること。JS側に付け忘れていて、JSが動くと属性が消える不揃いがあった。2026-08-03に修正)。**法令名の英訳は測ったうえで見送っている**: 公式訳(法務省「日本法令外国語訳データベースシステム」)があるのは対象49法のうち**18法=37%**だけで(2026-08-03に法令番号で全件照会。ほかに14法は「概要情報」がヒットするが、これは成立時の要旨であって条文の訳ではなく、法令名として使うと `(概要情報：令和6年5月成立)` のような説明文が名前の位置に出る)、当てると表が英日のまだらになる。しかも未訳が多いのは新しい法律のほうで、このページの対象は「直近5年の制定法」だから不利は解消しない。**一方で法令番号は訳ではなく換算なので英語化できる** — `令和四年法律第七十八号` → `Act No. 78 of 2022`(`hoan.js` の `lawNumEn()`)。**日本語表記は消さずに併記する**(法令番号は一次資料の識別子であって表示用の文字列ではないので、消すと e-Gov で引けなくなる)。この換算は `--check` が全行で検算する: 年は `promulgation_date` の西暦と、号は `law_id`(e-Govの法令IDは元号・年・号をそのまま埋め込んでいる)と突き合わせる。**検査が要るのは、`lawNumEn()` が読めない番号に対して null を返して併記を黙って省く設計だから** — 壊れても画面は「英語の番号が出ないだけ」で、JA側は元から出さないので何も変わらない。**最初は年しか見ておらず、漢数字の「十」の解釈を壊す負例が素通りした**(対象49法の年は三〜七の一文字で「十」を含まないため、年の検査だけでは号の壊れ方に触れない)。号の突き合わせはこれを受けて足したもので、負例3種(元号の基準年を1ずらす/「十」を壊す/「百」を壊す)がそれぞれ49行・48行・1行で落ちることを実測してある。**訳がある法律には、訳文そのものではなく公式英訳への「リンク」だけをEN側に併記する(2026-08-04)** — 表の見た目は日本語で揃えたまま、英語で読める一次資料への入口だけ渡す、という分け方。カバー率は測り直せる形にしてある(下の「公式英訳の照会」を参照)
- **公式英訳の照会はコードに焼き込まず毎回測り直す(2026-08-04)。** `bin/build_hoan.py` が e-Gov の巡回のついでに、法務省「日本法令外国語訳データベースシステム」(JLT)へ**法令番号で**照会して `translation_url` / `translation_note` を埋める。訳は後から追加されるので、一覧を書いた時点で古くなる(2026-08-04の実測は49法中18法=37%、うち暫定版1)。照会の要点は3つ: (1)検索は `https://www.japaneselawtranslation.go.jp/ja/laws/result/` へのPOSTで、`/ja/laws/search-no` から CSRFトークンと cookie を取る必要がある、(2)**検索範囲の「概要情報」(`ja=04`)を必ず外す**。既定で入っており、外さないと訳が無い法律まで当たる(概要情報は成立時の要旨であって条文の訳ではない)、(3)**暫定版**(JLT の "Tentative translation"＝校閲前)は「暫定版を含む」(`ia=03`)を外した検索で当たるかどうかで判定する。ページ本文の注意書きを読む手もあるが表題には何も付かないので、検索条件で切るほうが堅い。当たった翻訳ページは表題に入っている法令番号で照合してから採る(照合できなければ止める)。表示は**EN側のリンクだけ**で、置き場所は静的版・JS版とも**法令番号の行**に揃える(`hoan.js` の `transLinkHtml`)。条文見出しの側に置くとJS版では行を開くまで見えず、「訳がある」ことがクリックするまで分からなくなる。`--check` の `hoanTranslationErrors()` が語彙(空 or `暫定版`)とURLの形、そして**0件でないこと**を見る。0件を異常にしてあるのは、向こうのフォームの作りが変われば「1件も当たらない」形で静かに壊れうるからで、描画側は未知の値ならリンクごと黙って落とす設計なので画面には何も出ない
- **`index.html` の `#card-grid` にある `.card-fallback` を消さない。** `home.js` が実行時にカードへ差し替えるが、JSを実行しないクローラにとってはトップから各指標へ辿れる唯一の経路になっている
- **1ファイルで複数の指標を配信する形に戻さない。** かつて `chart.html` 1本を `?m=` で共有していたため、(1)固定canonicalが「8つは同じページ」という宣言になり7つが索引から消え、(2)本文が `—` と `----` のプレースホルダだけで、JSを実行しないクローラには読む中身が無かった。クエリ文字列ではファイルを出し分けられないので、この2つは指標ごとの実ファイルにしないと同時には直らない(2026-07-29に移行)

## 現在の状態(2026-07-23時点)

初期構築は完了し、運用フェーズに入っている。

- 公開中の指標: 8系列(実質GDP・名目GDP・税収・消費者物価・失業率・経常収支・国債発行額・国債発行総額) + 出生率ページ + 出生数ページ
- 実質GDPの見通しはFY1980まで遡って収集済み(FY1997以前は国会会議録の経済演説が出典)。名目GDPの見通しはFY1982〜FY1987を国会会議録の委員会質疑・政府答弁から、FY1989〜FY1997を『平成財政史』から、FY1988を『昭和財政史(昭和49〜63年度)』から収集済みで、**FY1980〜FY2025に欠測はない**。消費者物価の見通しも、経済演説が数値にふれないFY1989・FY1993〜FY1997を予算委員会での経済企画庁調整局長の説明から補って**欠測なし**
- ドメイン `zurekei.org` 開通済み、Cloudflare Pages で配信中
- アクセス解析: Cloudflare Web Analytics 稼働中(全ページにビーコン注入済み)
- 訂正履歴: 仕組みは実装済み(`data/corrections.csv` / `/corrections`)。訂正実績は現時点で0件

## TODO

### データ(開発)

- [x] FY1980〜FY1997の**実質**成長率見通し。内閣府アーカイブはFY1998始まりだが、経済企画庁長官の経済演説(国会会議録)に閣議決定された見通しの数値が読み上げられており、18年分すべてを一次資料から収集した。同じ演説から消費者物価の見通しも12年分収集
- [x] **FY1988の名目成長率見通しを埋めた(2026-08-04)。これで `forecast_nominal` の欠測は無くなった。** 会議録には数値言及が無く「『国の予算』昭和63年度版の現物か閣議決定文書原本が要る」と書いていたが、**財務省の『昭和財政史(昭和49〜63年度)』が本文で数値ごと記述していた**(第2巻 第15章「昭和63年度の予算」p.672。「昭和62年度の国民総生産伸び率(名目4.1%・実質3.7%[実績見込み])を上回る名目4.8%・実質3.8%の伸びが示された」)。**『平成財政史』で FY1989〜FY1997 を埋めたのと同じ道具が、その1つ前の期間にも存在していた** — 手前の期間で使えた資料の系列が前後に伸びていないかを先に見ること
  - **現物が要ると判断した根拠を疑うのが先だった。** 財務省の現行サイトでは `s49-63` シリーズのPDFが404で、目次ページだけが残って国立国会図書館WARPの保存版を案内している(WARPは自動取得にクッキー同意のHTMLを返すので、curlでは中身が取れない)。Wayback Machineには実体が1スナップショットだけ残っていた。**「一次資料はウェブに無い」は「現行サイトに無い」だっただけ**
  - スキャンPDF(400dpi・1bit)でテキスト層が無いため、`pdftoppm` で画像化して読んだ。OCRを挟むより、該当段落を切り出して300dpiで直接読むほうが速く、転記ミスも入らない
  - **裏取り**: 同じ記述の実質3.8%がCSVの既存値(経済演説から収集)と一致する。加えて国会会議録で大蔵大臣が「財政の中期展望」の前提を名目成長率4.8%と答弁している(1988-03-28 参議院大蔵委員会)。**ただし同書が記録しているのは昭和62年12月22日の閣議了解時点の数値**で、FY1998以降で採っている閣議決定版とは段階が1つ違う(実質はこの2段階で動いていない)。この但し書きはCSVの `notes` にも書いた
- [x] **FY1989・FY1993〜FY1997の消費者物価見通しを埋めた(2026-08-04)。これで `cpi_forecast.csv` の見通しも欠測は無くなった。** 「経済演説に数値がない」という記録自体は正しく(6年度とも本会議の演説には消費者物価の数値が無いことを全文で確認した)、**間違っていたのは「だから会議録には無い」と読み替えていたところ**。同じ閣議決定を**経済企画庁調整局長が予算委員会の冒頭で説明した記録**に数値が入っていた(例: FY1996は「国内総生産の実質成長率は二・五％程度に、消費者物価の上昇率は〇・五％程度になるものと見込まれます」1996-01-26 衆議院予算委員会 第1号)。**同じ資料群の中で、既に使っている文書の種類しか探していなかった**のが穴で、FY1988で『平成財政史』の1つ前の巻に当たったのと同じ形の見落とし
  - **裏取りは実質成長率との突き合わせ**(FY1982〜FY1988で使ったのと同じ方法)。この説明は実質と消費者物価を**一文の中で並べて**述べるので、実質側が `gdp_forecast.csv` の既存値(経済演説から独立に収集)と一致すれば、同じ文の消費者物価も同じ文書から読めていることになる。6年度すべてで一致した
  - **裏取りの厚みは年度でそろわない。** FY1989・FY1993・FY1994・FY1997は経済企画庁長官の所信や予算審議の質疑にも同じ数値が複数回出ており、翻刻ゆれの検出にも使えた(FY1993は予算委員会の本文が「ニ・一％」と片仮名のニで翻刻されており、他の3件が「二・一％」であることで読みを確定した)。**FY1995・FY1996は会議録中この1件しかない**ので、裏取りは実質との一致だけになる。内閣府のアーカイブはFY1998始まりで外部から当てる先も無い(`mitoshikako.html` の一覧を実際に取って確認した)
  - **判定材料が1件しかない年度は、そのことを書き残す。** 「6年分埋めた」とだけ書くと裏取りの厚みの差が消える
- [x] 実績値のヴィンテージ(基準改定)の表現・Phase 2第一段。基準別の後年推計系列(`data/gdp_vintages.csv`、平成2年〜2015年の6基準)を収集し、実質/名目GDPページ背景に「包絡帯」(各年の基準別最小〜最大)として可視化。実績が基準改定でどれだけ動いたかの幅を示す(例: FY1985実質は4.1〜6.3%、2.2ポイント幅)。Phase 1(`actual_basis`列・破線＋シーム線)と合わせて対応済み
- [x] **実績値のヴィンテージ・Phase 2本丸(2026-08-06)。初回確報(当時公表された実績)を `data/gdp_first_release.csv` として23年分collect し、実質/名目GDPページに細い点線で重ねた。** これで見通しとのズレが「予測を外した分」と「実績が動いた分」に分解できる。効きは実データで確認済み: **FY2021は見通し4.0に対し当時の実績が2.5(−1.5の大外し)だったが、最新確報は3.9でズレは−0.1**。見通しは1文字も変わっていないのに実績のほうが動いて「ほぼ的中」になっている。FY2014も−2.4→−1.1と半分以下になる。実質の改定ドリフトは平均0.51ポイントで、見通しの外れ幅と同じオーダー
  - **「現物が必要」と書いていた前提が、最近側については誤りだった。** 内閣府は確報を**年版ごとのディレクトリ**(`kakuhou/files/h30/` など)で保持しており、年版`hN`の最新年度=平成N年度が、その年度の初回確報にあたる。この読み方は推測ではなく2003-12-01の公表資料が明記している(「毎年12月に前年(度)の数値を『確報』として…公表している」)。**FY1988・消費者物価に続いて3回目の「現物が要るという判断のほうが間違っていた」**
  - **手がかりは既にCSVの中にあった。** `gdp_vintages.csv` の出典URLが `.../files/**h10**/tables/...` という形をしていて、年版ごとのディレクトリの存在を示していた。**新しい資料を探す前に、手元のデータが出典として何を指しているかを見ること**
  - **一覧に載っていない年版が生きている。** ディレクトリ探索は403(存在するが一覧禁止)と404(不在)で判別できる。h16は**トップページだけが404で統計表は200**だった(一覧から辿ると存在しないように見える)。逆にh11〜h14は現行サーバーにもWaybackにも無く、**FY1999〜FY2002は埋まらなかった**
  - **裏取りは3段階で厚みが違う**(行ごとに`notes`に書いた)。FY2010〜FY2024は同時公表の「ポイント」と**15年全部一致**、FY1998・FY2003・FY2009は`gdp_vintages.csv`と一致、FY2004〜FY2008は**統計表1本だけ**
  - **統計表は公表後に差し替わりうる**(正誤情報にFY2018・FY2019・FY2020の記載)。「今その年版として配信されているもの」は公表当日のバイト列とは限らない。該当年度が「ポイント」と一致したことでこの経路の値は成長率に及んでいないと判断したが、**この区別自体を`data/README.md`に書き残した**
  - **化けたテキスト抽出を根拠にしない。** FY2020の「ポイント」はフォント埋め込みの都合で`pdftotext`が化ける。図中の数値がたまたま符合して見えたが、`pdftoppm`で画像にして直接読み直した(『昭和財政史』のときと同じ手)
- [ ] **為替のテーマは「載るものが無い」で一度閉じた(2026-08-06)。同じ検討を繰り返さないために理由を残す。** 円安・為替介入が話題になったときに候補を洗ったが、**為替について見通しを公表している公的機関が日本に存在しない**というのが結論。
  - **政府経済見通しの円相場前提は載らない。** 別添「主要経済指標」の注2が「これらは、作業のための想定であって、**政府としての予測又は見通しを示すものではない**」と明記しており、備考も「令和6年11月1日〜11月30日の期間の平均値(153.6円/ドル)で、同年12月以降一定と想定」= **判断がゼロの機械的規則**。ここを測っても出てくるのは為替の変動そのもので答え合わせにならない(現物のPDFで確認済み)
  - **財務省の外国為替平衡操作(介入)も載らない。** 実績は1991年4月から揃うが、**事前の数字が構造的に存在しない**(介入は事前告知しない)。「見通し vs 実績」の枠に入る余地が無い。支出官レートも政府前提と同じ理由で不可
  - **日銀短観の想定為替レートは載るが採らない。** データは綺麗(調査側と実績側が同じ表)だが、**民間企業の計画前提の集計**で、項目名からして予測ではない。既存の全ページが「公的機関が自分の名前で出した数字」で貫かれているところに、初めて「誰の判断も問えないページ」が混じる
- [x] **日銀「展望レポート」の見通し vs 実績(段階1: 2026-08-06公開、段階2: 同日公開)。** `/chart/boj-outlook-real`・`/chart/boj-outlook-cpi`(段階1)、`/boj-outlook-vintages`(段階2)。政府の経済見通しとは別に、日銀自身が示す見通し。2000年10月の第1回から全号が恒久URLで残り、各号の「政策委員の大勢見通し」が実質GDPと消費者物価(除く生鮮食品)の見通しを幅+中央値で載せる。**2013年4月号が異次元緩和の直後に2015年度の物価を(消費税を除いて)+1.9%と見通していた**記録がそのまま残っており、外し方の大きさは既存ページより1桁大きい
  - **段階1は扇形(`/fertility`・`/births`)構造には乗せず、既存の「見通し・実績・ズレ」2本線パターンで出した。** 扇形(1つの対象年度に対し号を追うごとに見通しが改定される構造)は将来の段階2として温存し、まずは「当初見通し」1本を既存METRICSパターンにそのまま載せる形にした。判断はユーザーと相談して決定(段階を分ける／4月号に統一する の2点)
  - **「当初見通し」は毎年4月号(=年度開始と同月に公表された号)の中央値に統一。** 展望レポートは2000〜2015年度が4月・10月の半期、2016年度以降は1月・4月・7月・10月の四半期で発行頻度が変わる。政府見通し(年度開始の約3か月前の1月に決定)に倣って「直前の号」を採ろうとすると2016年度より前は1月号が存在せず、号のホライズンが年度によって6か月〜2年とバラつく。全期間でホライズンを揃えられる号は4月号だけなので、政府見通しとタイミングの意味が違う点を注記した上でこれを採用
  - **実績は`gdp_forecast.csv`/`cpi_forecast.csv`の同一年度の値をそのまま転記。** 日銀自身は実績を公表しない(展望レポートは見通しのみ)ため。転記元が改定されたら追随が必要(自動同期の仕組みは無い)
  - **収集した生データ(406行、全号・全対象年度・再掲行を含む)は`data/boj_outlook_vintages.csv`としてリポジトリ化した(段階2)。** `data/boj_outlook_real.csv`/`boj_outlook_cpi.csv`は4月号・main行だけを抜いた26行の段階1用データのまま
  - **段階2は`/boj-outlook-vintages`ページで、対象年度ごとに号を追った見通しの推移を扇状チャートで見せる。** フェア(`/fertility`・`/births`)と同じ「歴代の予測を重ねる」構造だが、軸の取り方が逆になる: フェアは1本の推計が数十年先まで伸びるので**x軸=対象年**でvintage(推計版)ごとに線を引けるが、展望レポートは1号が1〜3年先までしか見通さないため同じ取り方だと74本の2〜3点の線になり読めない。そこで**対象年度でグルーピングし、x軸=号の公表時期**にした(1つの対象年度に対し、その年度を見通した号の系列が短い斜め線になる)。全26対象年度が2000〜2028年の時間軸上に重ならず並ぶ「カスケード」状になる
  - **チャート・表とも`main`行のみ使い、`restated`(再掲)行は表示しない。** 再掲時に前提が読み替えられることがあるため(下記)。`chart/*.html`の汎用METRICSエンジンではなく、`fertility.html`/`births.html`と同じ手書きページパターン(`bin/build.mjs`の`injectRegion()`で領域だけ差し替え)で実装した — 構造がMETRICSの想定(1指標=見通し1本+実績1本)と異なるため
  - **ホームページのカードグリッド・DataCatalogには載せない。** 段階1の2ページ(`/chart/boj-outlook-real`・`-cpi`)は既存METRICSの一部としてホームカードを持つが、段階2は段階1の詳細版という位置づけで独立のホームカードを設けていない。`fertility`/`births`/`hoan`と違い、JSON-LDに`includedInDataCatalog`を持たせていない(ホームカードを持つページだけがDataCatalogに参加する、という既存ルールをそのまま適用)
  - **転記の検算が原資料に内蔵されている。** 各号が前号の見通しを再掲するので、再掲行と本体行を突き合わせれば転記ミスが落ちる(出生数で「出生−死亡=自然増加」を一緒に写したのと同じ手)。**ただし4月号だけを集めると再掲先が10月号/1月号になり照合相手がデータセットに入らない** — 実際に一度これで検算が空回りし(0/67件)、その状態で転記ミスを1件見逃した。**全号を集めて初めて検算が効く**(段階1のCSVは4月号のみだが、収集自体は全号を集めてから絞り込んだ)
  - **再掲行を「当時の値」として使ってはいけない。** 2019年1月号は2018年10月号の物価見通しを+1.5〜+1.7<+1.6>として再掲しているが、10月号自身が印刷したのは+1.8〜+2.0<+1.9>。教育無償化政策の前提が後から加わったためで、**日銀は再掲時に過去の見通しを新しい前提で読み替える**。当時公表された値は各号の本体行のほうにある(実績側の改定ドリフトと同じ構造が見通し側にもある)
  - **消費税調整の列は意味が変わる。** 2018〜2020年頃は「消費税率引き上げ・**教育無償化政策**の影響を除くケース」、**2022年以降は「除く生鮮食品・エネルギー」(コアコア)で消費税とは無関係の別概念**。同じ位置の列を機械的に読むと混ざる
  - **中央値が無い号がある。** 2000-10〜2002-10(FY2001・FY2002の4月号)は中央値の公表前、2020-04(FY2020)はコロナ下の不確実性を理由に号全体で中央値を出していない。段階1のCSVでは3年度とも`forecast_real`/`forecast_cpi`が空欄(レンジのみ収録)
- [ ] FY1999〜FY2002の初回確報。平成11〜14年度確報の年版が内閣府のサイトに無く(h11〜h14のディレクトリが404)、Wayback Machineにも痕跡が無い。平成14年度確報の公表資料(`seibi/kouhou/contents/sokyuukaitei.html`)は残っているが公表スケジュールの記載だけで数値が無い。FY1997以前と同じく現物か別経路が要る。急がない
- [x] FY1993以前の見通しはGNPベースで実績(GDP)と概念が異なる。チャートに`forecast_basis`列を持たせ、GNPベース期(FY1980〜1993)を淡い背景帯＋「GNPベース期」ラベルで示し、年別読み出しに「見通しの基準」を追加、注記でも明示。実績ヴィンテージのシーム(FY1994/1995)とは別レイヤー(帯=見通し概念/線=実績基準改定)として1年ずらして描き分けている
- [x] **出生数(人数)の系列**(2026-08-04に `/births` として公開)。`/fertility` が持っているのは合計特殊出生率だけで、年間の出生数そのものは扱っていなかった。社人研の歴代推計7版(1992〜2023)の出生数推計(出生中位)と、人口動態統計の実績(1980〜2025年)を集め、`data/births_forecast.csv`(340行) / `data/births_actual.csv`(46行)にした。生成は `bin/build_births.py`。人口動態統計の概数公表は毎年6月上旬で、報道の見出しは出生率ではなく出生数で立つ
  - **いちばん重要な発見は「推計側に2つの基準が混じる」こと。** 社人研が出生数を載せている表(表1-8系)は2012年版までが総人口ベース(脚注に「日本における外国人を含む．」)で、2017年版・2023年版だけが日本人人口ベースの表1-8(J)を併せて公表している。実績は人口動態統計の「日本における日本人」しか完全な年次系列が取れないので、**基準が厳密に揃うのは2017/2023の2版だけ**になる。総人口ベースの実績で揃える案も当たったが、人口統計資料集の総数は基準年しか無く断念した。差は2021年の実測で17,881人(出生数の約2.2%)＝各版のズレより1桁小さいので、**混ぜずに明示する**方を採った: `forecast_basis` 列を持ち、総人口ベースの版のラベルに「＊」を付け、`scopeNote` に差の実測値を書く。語彙は `birthsBasisErrors()` が検査する(負例3種——語彙外の値・1つの版の中で基準が混在・`births.js` の `BASIS_JAPANESE` が語彙の外——がそれぞれ落ちることを実測済み)
  - **1992年版だけは原本がスキャンPDFで目視の書き写しになる。** 転記ミスを機械で捕まえるため、同じ表から出生数・死亡数・自然増加数の3列を写し、**全35年分で「出生 − 死亡 = 自然増加」が成り立つことを `bin/build_births.py` が生成時に検算する**(丸めのため±1まで許容)。1桁でも読み違えれば恒等式が崩れる。数値そのものを検算する手段が無いときは、**原資料が持っている別の恒等式を一緒に写す**のが手になる
  - **高位・低位は収録していない。** ページが並べるのは中位だけなので、収録すれば「使わないのに人手で転記した数値」が3倍になる。転記量は誤りの量なので、使わないものは取らない
  - **専用のOGカードは公開時には持たせず、汎用の `assets/og.png` で先に出した(2026-08-04に専用カードへ差し替え済み)。** 焼き直しの単位が22枚一括なので、ページの公開と切り離せる作業として後回しにしたもの。実際に足すときは `og.html` の `renderFertility()` を `renderFan(spec)` へ一般化し(扇形2ページの差分は `FAN_SPECS` に寄せた)、`bin/build.mjs` 側も `fertilityCardInfo()` → `fanCardInfo(fan)` と `FAN_CARDS` に一般化した。**種別名 `fertility` → `fan` の改名もこのとき行った**(種別が表しているのは扇形の描画かどうかであってページ名ではない)
  - **`births.js` は `fertility.js` の描画部分(`svgEl` / `buildSegments` / 軸・ラベル衝突回避・描き込みアニメーション、約110行)をあえて複製している。** 共通化すると2ページが1ファイルに依存し、片方の見た目を触るともう一方に必ず影響が出る。扇形が2枚のうちは影響範囲が閉じている側の価値が上回ると判断した。**3枚目の扇形ページを作るときに共通化する。** 危ないのはコードの重複より「同じ文字列・同じ数値が2箇所にある」重複(下の「検査の穴」)で、辞書もCSVも別物のここはそれに当たらない

- [x] **`gdp_vintages.csv` に行ごとの出典URL列がない**(2026-08-04に解消)。数値を載せているCSVの中でこれだけが `data/README.md` §1「すべての数値行に一次資料へのURLを付す」を満たしていなかった。**横持ちのままURL列を足す形は採らなかった** — 基準×実質/名目で12本のURL列が要るうえ、1つの基準の出典は全年度で同じファイル1本なので、同じ文字列が数十行に並ぶだけになる。1行=(年度 × 基準)の**縦持ち**に組み替えて、1行が値＋出典＋`notes` という他ファイルと同じ形になるようにした(54行13列 → 137行7列)。**基準が増えたときに列もコードも増えないのが、この形にした決め手**(次の2020年基準は `gdp_forecast.csv` 側で既に使われており、来ることが分かっている)。`chart.js` 側は6列を束ねる `vintageCols` が1列を指す `vintageCol` になり、年ごとに畳む処理に変わった(描画結果は不変。旧版と新版のヘッドレスChromeのDOMで `vintage-band` のパスがバイト単位で一致することを確認した)。合わせて (1) 基準の呼称を `T.basisLabels`(実績線の基準表示と同じ辞書)に統合し、**辞書に無い `basis` が来たらビルドを止める**(負例: `1990base` を `2020base` に置換して throw を確認)、(2) 帯の出典6本を**静的HTMLに出す**ようにした(`bin/build.mjs` の `vintageSourceHtml`。JSでは組み立てない)。**この2つ目は `/fertility` の出典欠落と同じ形の穴で、実際に開いていた** — `sourceUrlReachErrors()` は既にこの検査を持っていたが、当時の `gdp_vintages.csv` にURL列が無かったので検査対象が存在せず、素通りしていた。出典ブロックを外す負例で12本中10本が「静的HTMLに無い」と報告される(残る2本は `gdp_forecast.csv` の `2015base-ref` 行と同一URLで、そちらの経路で既に出ている)。**移行にあたって12本の統計表を取り直し、全274セルを一次資料と突き合わせた(相違0件)**

- [x] **`/births` の専用OGカードを足した(2026-08-04)。** `assets/og-births.png` / `og-births-en.png` の2枚が増えて全20枚→22枚。**2枚足すのではなく、扇形カードの経路そのものを一般化した**: `og.html` の `renderFertility()` を `renderFan(spec)` にし、2ページの差分(CSV2本・値の列名・タイトル・y軸の定義域と刻みとラベル書式)を `FAN_SPECS` に寄せた。`?m` の予約値の一覧もこのオブジェクトが唯一の出所で、`main()` は `FAN_SPECS[m]` を引くだけになった(予約値を別途列挙しないので「キーはあるのに描けない」状態を作れない)。`bin/build.mjs` 側も同型に `fanCardInfo(fan)` + `FAN_CARDS` へ一般化し、`OG_INPUT_RELS` の扇形CSVも `FAN_CARDS` から機械的に導くようにした(扇形ページを足せば陳腐化検査の対象に自動で入る)。**種別名は `fertility` → `fan` に改名した** — 種別が表しているのは「どのページか」ではなく「扇形の描画か」で、扇形が2枚目になった時点でページ名では内容と合わなくなる。
  - **既存20枚が1バイトも変わらないことで「他に影響していない」を確認した。** レンダリングは決定的なので、22枚を焼き直したあとの `git diff` に出るのは新規2枚と `assets/og.inputs.json` だけになる。目視が要るのは増えた2枚だけで済む(22枚を見比べるより強い確認)
  - **列名の綴り違いだけは両側が揃って素通りしうる穴があったので塞いだ。** CSVパスと値の列名は `og.html` の `FAN_SPECS` と `bin/build.mjs` の `FAN_CARDS` の両方に書いてある(og.html はHTMLなので `loadModule()` で読めず、辞書と違って共有経路が作れない)。値がずれれば `vintageCount` の期待値と実際の描画が食い違って必ず赤くなるが、**列名を両側で同じように綴り間違えると `toNum()` が存在しない列を `null` にするので両側とも0本と数え、`0==0` で通ってしまう**。両側とも「その列がCSVのヘッダに実在するか」を先に見て投げるようにした
  - **出生数カードは `＊`(総人口ベースの版に付く印)を描かない。** カードは版ラベルを一切描かないので印を付ける相手が居ない。基準の差(2021年で約1.8万人)はこのカードのy軸では1px相当で線としては見えず、数値も一切書かないため、カードだけを見て基準を取り違える読み方が生じない。基準の話はページ本文の `scopeNote` が唯一の出所であり続ける

- [x] **`/hoan` のEN側に、公式英訳へのリンクを併記した(2026-08-04)。** 法令名は訳さないまま、訳がある法律にだけ `official translation ↗` を出す。照会は `bin/build_hoan.py` の巡回に組み込んで毎回測り直す形にしてあり、結果は焼き込まずCSVの `translation_url` / `translation_note` に入る。仕組みと落とし穴は上の「公式英訳の照会」を参照。実装して分かったこと2件 — (1)**暫定版が実在する**(令和七年法律第四十九号。見つけたのは `ia` を外した検索と外さない検索の差分で、当初は「暫定版は無い」と見立てていた)。JLT自身が校閲前だと断っている区別なので、こちらでも `(tentative)` と出す。(2)**JLTの英語側の法令番号表記は当てにできない** — 令和三年法律第二十五号(相続土地国庫帰属法)を `Ministry of Justice Order No. 25 of 2021` と表記している。法律を府省令として出しているので、`lawNumEn()` の検算にJLTの表記を使う案は採れない(検算は今も `promulgation_date` と `law_id` との突き合わせでやっている)

### 検査の穴

2026-08-01のレビューで、どの検査にも掛からない手書きの複製が見つかった。いずれも当時持ち込まれたものではなく元からある。片方だけ直すと黙ってズレるという点で「同じ文字列を2箇所に置かない」の未達例。**5件とも2026-08-04に塞いだ**(それぞれ負例で落ちることを実測してある)。

**検査を足したこと自体で実際の欠陥が2件見つかっている** — `hoan.html` の古い `og:description` と、`/fertility` の出典がJSでしか出ていなかった件。後者は切り出したときの見立て(「複製がある」)自体が間違っていて、手を付けて初めて分かった。**穴として書き留めた記述も、直すときに必ず実物で確かめ直すこと。**

- [x] **`ASSET_V` の複製。** `bin/build.mjs` の `ASSET_V` と、手書きHTML各ページの `?v=` は別々に書かれていた。実験では `about.html` の `style.css?v=` だけを古い値に戻しても `--check` は緑のまま通った(取り違えると、そのページだけ古いCSS/JSをブラウザが使い続ける)。`assetVersionErrors()` が全HTMLの `href`/`src` に付く `?v=` を `ASSET_V` と突き合わせる。**対象を属性に限っているのは、`og.html` に「`?v=` を付けない理由」を説明したコメントがあり、素の `?v=` を拾うと誤爆するため。** 0件もエラー(fail-closed)
- [x] **`hoan.html` / `fertility.html` が手書きJAページの文言検査の対象外だった。** `handwrittenJaDriftErrors()` が見ていたのは `about` / `corrections` / `contact` / `cite` だけ。2026-08-01に `hoan.js` の `T.ja.desc` だけを直して `hoan.html` の静的本文を直し忘れ、**同じページの中で本文(旧)とJSON-LD(新)が食い違ったまま `--check` が緑で通った**(JSが動く環境では `set("t-desc", t.desc)` が上書きするので永久に気づけない)。この2つは「全体が生成物」ではなく `injectRegion()` で一部の領域だけを差し替える形なので、**それ以外の地の文は他の4ファイルと同じ手書き**であり同じ壊れ方をする。対象に追加した。**このとき `injectRegion()` のマーカーが差し替え先の要素の中に入る**(`<p id="fert-scope-note"><!-- BUILD:scope -->本文<!-- /BUILD:scope --></p>`)ため、落とさずに比べると生成側とJS側が同じ `T.ja` を見ていても必ず食い違う(実際に `#fert-scope-note` が誤検出になった)。落としてから比べることで、生成領域の中身が `T.ja` と一致しているかまで見る検査になっている
- [x] **JAトップの `card-fallback` のリンク文字列。** `home.js` の `INDICATOR_META.nameJa` を改名しても、`index.html` の素のリンクは手書きのまま取り残されていた(EN側は生成物なので自動で追随し、JA/ENが非対称になる)。2026-08-01のCPI改題で実際に取り残した。`cardFallbackErrors()` が href と表示文字列を**並び順込みで**突き合わせる(集合で比べると入れ替わりを見逃す)。件数の不一致・0件もエラー
- [x] **`og:description` が無検査の手書き複製。** `buildHome()` は `index.html` の `<meta name="description">` だけを原本としてJSON-LDへ流しており、`og:description` は誰とも突き合わせていなかった(EN側は `buildHomeEn()` の const 1箇所から meta/og/JSON-LD すべてが生成されるので複製が無い)。同じ複製はトップ以外の手書きJAページにもあるので、`ogDescriptionErrors()` は全ページを見る。**検査を足した時点で実際に1件見つかった**: `hoan.html` だけ `og:description` が古い短い版のまま残っており、**JA側に説明文が3種類**(meta description / og:description / JSON-LD)存在していた。EN側は生成物なので1つに揃っている。`og` を `meta` に揃えて解消した。なお `meta description` と JSON-LD の `description` まで一致は要求していない — 検索スニペット向けの短い説明とページの説明が違うのは正当で、ここで潰したいのは「同じ役割の文字列が2箇所にあって片方だけ古くなる」ほうだけ。片方だけ存在する場合もエラー(`og` だけ消える壊れ方はSNSカードにしか出ないので目視では気づけない)

- [x] **`/fertility` の出典行(`#fertility-source`)がJSでしか組み立てられていなかった(2026-08-04に解消)。** ここは当初「静的版とJS版が別々に同じものを組み立てている複製」として切り出したが、**その見立てが間違っていた**。実際には静的版が存在せず、`fertility.js` の `buildSourceHtml()` だけが原本で、生成HTMLの `<div id="fertility-source">` は空のまま `<noscript>` で畳まれていた。つまり複製ではなく**欠落**で、しかも「出典つきで確かめられる」というこのサイトの前提そのものに当たる。JSを実行しない相手には**実績値の出典3件と、2002年推計の「原本はスキャンPDFなので国立環境研究所のミラーで数値を確認した」という但し書きが1文字も届いていなかった**。推計側7件のURLだけは上のズレ一覧が別途持っていたため、ページを見ても「出典が1件も無い」ようには見えず、気づく手がかりが無かった。数値表と同じ扱い(ビルドがHTMLに埋め、JSでは組み直さない)に変え、`#fertility-source` を `<noscript>` の対象から外した。`buildSourceHtml()` は削除し、原本は `fertilitySourceHtml()` の1つだけになった
  - **`#fertility-legend` のほうは問題なし。** JSが描くSVGの凡例で、SVGごと畳まれるので静的版が要らない。ここに複製は無い
  - 再発防止に `sourceUrlReachErrors()` を足した。**`data/*.csv` のURL列に載っているURLは、サイトのどれかの静的HTMLに現れなければならない**(実測212件)。ページ単位ではなくサイト全体で見るのは、`gdp_forecast.csv` の `実質:URL 名目:URL` という複合セルが2ページに分かれて出るため。**比較の前に `&amp;` を戻すこと** — e-statのURLはクエリを持つのでHTML側は必ずエスケープされており、忘れると誤検出する(負例で確認済み)。この検査は事故当時の状態を再現すると、まさにその3件を挙げて落ちる
  - **この形の検査は `/chart/*` では既に満たされていた。** あちらは表の行ごとに出典リンクが付くので、静的HTMLに全URLが出る。`/fertility` だけが落ちたのは、表が「年 × 推計版」の行列で**行ごとの出典欄を持てない**形だから。表の形が変わったページを足すときは、出典の載せ場所を先に決めること

### 運用(人間の判断待ち)

- [x] サイト名: ズレ計 (確定)
- [x] ドメイン取得
- [x] 問い合わせ窓口をサイトに掲載するか。掲載すると決め、`/contact` のフォームを全ページのフッターとAboutページから導線を張った。返信も行う(受信と送信で別経路を使い、`info@zurekei.org` 名義で返信できる状態にしてある)
- [x] `about.md` の署名・問い合わせ手段の追記。「運営は一個人です」と明記し、問い合わせフォームへ誘導する形に置き換えた

### 年次の定常作業

- 12〜1月: 新年度の政府経済見通し(閣議決定版)を各CSVに追加
- 7〜12月: 決算・確報の公表に合わせて実績値を更新
- 不定期: 社人研「日本の将来推計人口」の新版が出たら `fertility_forecast.csv` に vintage を追加

## 初期構築タスク(完了済み・履歴)

1. 内閣府アーカイブから過去の政府経済見通し(当初)の成長率予測値を収集し、`data/gdp_forecast.csv` を作る
   - PDFからの転記になるため、必ず出典URL・ページ番号をCSVに残す
   - 先行研究の数値と突き合わせて転記ミスを検出する
2. 国民経済計算から実績値(確報)を収集して同CSVに結合
3. CSVを読んで描画する最小のグラフページを作る(年選択→予測線アニメーション)
4. ここまでをプレビューデプロイして人間がレビュー

## 経緯メモ(なぜこのプロジェクトか、一行で)

政策のズレの上流には「予測が外れても誰も記録しない=学習ループの不在」がある。独立検証機関(日本版OBR)ができるまでの民間先行版として、権限ゼロでできる「予測と実績のズレの継続記録」を一市民が作る。
