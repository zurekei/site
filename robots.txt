# ⚠ このファイルは、配信時に Cloudflare の Managed robots.txt が「前に」連結される。
#   つまりここに何を書いても、上に置かれた規則を消せない。2026-07-29 時点で上側は
#   ClaudeBot / GPTBot / CCBot / Google-Extended などに Disallow: / を出しており、
#   AIに引用されるという方針と正面から衝突している。外すのは Cloudflare ダッシュボード
#   側の設定で、このファイルの編集では不可能。実際の配信内容は必ず本番で確かめること:
#     curl -s https://zurekei.org/robots.txt
#
# AIクローラは意図的に許可する方針。このサイトの目的は参照されることなので、
# 検索・回答への引用は歓迎する。
#
# 下の User-agent: * は省略できない。これが無いと Disallow: /og が、上の Cloudflare
# ブロックの最後のグループ(meta-externalagent)への規則として読まれてしまう。
User-agent: *
Allow: /

# OGP画像を書き出すための作業用ページ。読み物ではないので索引に入れない。
Disallow: /og
Disallow: /og.html

Sitemap: https://zurekei.org/sitemap.xml
